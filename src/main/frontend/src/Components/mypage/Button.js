import './Button.css';

const Button = () => {
    return (
        <a href="/#" className="btn-two cyan rounded">
            <button onClick={() => {
            }}>
                버튼
            </button>
        </a>
    );
};

export default Button;